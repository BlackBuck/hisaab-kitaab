import { Lusitana, Inter, EB_Garamond } from "next/font/google";

export const garamond = EB_Garamond({subsets: ['latin'], weight: '400'});
export const inter = Inter({subsets: ['latin']});
export const lusitana = Lusitana({subsets: ['latin'], weight: '400'});